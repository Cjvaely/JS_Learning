/**
 * @authors Chen Cjv (cjvaely@foxmail.com)
 * @date    2018-08-14 01:44:05
 * @version $Id$
 */

/*
条件判断：

JS使用的是if() {....} else {...}做条件判断
JS把 null undefined 0 NaN和空字符串''当作false
其它都是true

循环：
for (var i = v1; i<= v2; i++) {

	}

var o = {
	name:'jack',
	age:20,
	city:'Beijing'
}
for (var k in o) {
	console.log(k);		// 'name', 'age', 'city'
}
// 要过滤掉对象继承的属性，用hasOwnProperty()来实现：
for (var key in o) {
    if (o.hasOwnProperty(key)) {
        console.log(key); // 'name', 'age', 'city'
    }
}
 
while( ){
	
}

do {
	
} while;
*/